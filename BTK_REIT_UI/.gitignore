# UI-specific ignores
# Common patterns (bin/, obj/, .vs/, secrets, etc.) are handled by the root .gitignore.

# Blazor WebAssembly framework files (generated on publish, not source)
wwwroot/_framework/

# Local appsettings overrides
appsettings.Development.json
appsettings.Local.json
